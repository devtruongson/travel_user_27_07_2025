import TestForgotPassword from "../(auth)/test-forgot-password";

export default function TestPage() {
    return (
        <div className="min-h-screen bg-gray-100 py-12">
            <TestForgotPassword />
        </div>
    );
}
